#include <iostream>
#include <string>
#include <cstring>
using namespace std;

int main()
{

    // string s = "Debopam Chowdhury has a bad sleep Cycle . But he is trying to fix it.";
    // cout << s.length() << endl;

    // string s1 = "Debopam Chowdhury";
    // cout << s1.max_size() << endl;

    // string s="Hello ";
    // s.append("World. ");
    // cout<<s<<endl;

    // string s = "Hello";
    // cout << s.capacity() << endl;

    // s.append(" people. How are you all doing . Great!");
    // cout << s.capacity() << endl; // Size of string adjusts automatically

    // string s="How you?";
    // s.insert(3," areas",4);
    // cout<<s.length() << " "<<s<<endl;

    // string s = "How are you? ";
    // s.replace(4, 3, "good are");
    // cout << s << endl;

    // string s="Hello ";
    // s.push_back('g');// use single quotes only & 1 character can be used only
    // cout<<s<<endl;

    // string s="Hello";
    // s.pop_back();
    // cout<<s<<endl;

    // string s1="Programming" , s2="language";
    // s1.swap(s2);
    // cout<<s1<<endl;

    // string s="Hello Bhaiyaji . Kaise hain aaap?  Bhabhi kaisi hain?";
    // char p[20];    //size of the array doesn't matter . It will automatically resize if bigger
    // s.copy(p,s.length());
    // cout<<p<<"   "<<strlen(p)<<endl;

    // string s="Main bola hey , woh mani toh , do pal saath beetayein to , hey";
    // cout<<s.find('z')<<endl; //When char/string is not found , it outputs the Max_Size
    // cout<<s.find("hey")<<endl;

    // string s="Hello World";
    // cout<<s.find_first_of('l',2)<<endl;// 2 --> Index fro where we may start looking
    // cout<<s.find_last_of('l')<<endl;

    // string s = "Programming";
    // cout<<s.substr(3,4)<<endl; // 3 - starting index , 4 - no. of characters to be given out

    return 0;
}